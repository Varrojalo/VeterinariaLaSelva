<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cl.inacap.veterinarialaselva">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.Principal"
            android:label="@string/title_home"></activity>
        <activity
            android:name=".view.VistaCatalogo"
            android:label="@string/title_catalog">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.VistaUsuario" />
        <activity
            android:name=".view.Carga"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".view.InicioSesion"
            android:label="@string/app_name"></activity>
        <activity android:name=".view.VistaAnimal" />
        <activity android:name=".view.Registro" />
        <activity android:name=".view.Adopcion" />
        <activity android:name=".view.main"></activity>
    </application>

</manifest>